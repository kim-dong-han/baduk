# TODO

> 단일 상태 소스(single source of truth). 진행 현황은 여기서만 관리.
> 작업 끝나면 진행중→완료로 이동(요약 1줄). 길어지면 완료 오래된 건 잘라낸다.

## 진행 중
- (없음)

## 방향 (2026-07-03 확정)
- **배포 중단** — 개인 GPU 연산 부담. 이 저장소=포트폴리오, 엔진 로컬, b28 고정. 배포판은 추후 별도 저장소. (PROJECT.md 프로젝트 성격 참조)
- 초점: **세부 페이지 콘텐츠 고도화**.

## 예정 / 후보 (페이지 콘텐츠, 우선순위 ⭐)
- **README 스크린샷 4장** (사용자 직접 촬영 필요) — `docs/images/` 가이드대로. 정적 데모/헤드라인용 히트맵은 pro `c9ba1000` 종반이 선명
- **GitHub Pages 켜기** (사용자 액션) — Settings→Pages 소스 main/docs. 정적 파일은 docs/에 준비됨
- `.card` box-shadow 통합은 HTML 공용 클래스 필요해 보수적 보류 유지

## 최근 완료 (최신순, 5건 유지)
- **갤러리·오답노트·소개 3개 화면 리디자인**: 시안 기준으로 전면 재구성, 각각 전용 CSS(gallery/notes/about.css) 신설, common.css 미사용. **갤러리**: 번들 사활 75 + 분석 기보 31 = 106장을 실제 국면 캔버스로 렌더(사활은 region 확대, 기보는 previewMoves로 최종 국면). 시안의 조회수·좋아요·업로드·작성자는 없는 데이터라 제거하고 정해 수순·급소 수·AI 일치율로 대체. **오답노트**: 실제 MistakeNote 131건, 실수 지점을 빨간 원으로 강조한 국면, 복습 상태(브라우저 저장) 도넛·필터, 복습하기는 `?move=` 딥링크. **소개**: 통계 4칸을 실제 값(31판/75개/131건/48.2%)으로, 엔진 visits도 서버 값. 백엔드 추가: `TsumegoService.all()`, `GalleryItem.previewMoves`, `MistakeNote.previewMoves`, AboutController 통계. 검증: 9개 페이지 200, 템플릿 예외 0, 1600px 렌더 확인.
- **실시간 분석(/study) 전면 리디자인**: 2열 판+패널을 3열 연구 대시보드로 재구성. `static/css/study.css` 신설, common.css 미사용, 헤더는 `fragments/appheader`. /play(대국)와 톤·정보 구조를 명확히 분리. **기존 판 JS 유지**(13개 id 전수 확인) + 5곳 보강. 시안의 가짜 데이터(고정 폴리라인·38/29/17/11/5% 도넛·45수 R3 -6.3%·Q16 추천 설명문)는 전부 실측으로 대체: 승률 그래프·집차 추이는 착수마다 누적한 실제 분석값(캔버스 직접 렌더), 수 품질은 집손해를 프로젝트와 동일 임계값으로 등급화, 추천수 TOP5는 `/api/analyze/top` candidates(설명 컬럼은 백엔드 미제공이라 삭제, 행 클릭 시 예상 진행 표시), 하락 TOP3는 수별 승률 낙폭. 없는 항목(AI 레벨·규칙·제한 시간)은 삭제하고 분석 엔진·visits·덤·판 크기로 대체 — visits/덤은 `KataGoService.TOP_VISITS/KOMI` 상수를 추출해 컨트롤러가 주입(하드코딩 중복 제거). 신규: 좌표 표시 토글. 검증: 실제 `/api/analyze/top` 응답 확인 + 스텁으로 6수 진행 렌더, 1600/1100px, 9개 페이지 200.
- **AI 대국(/play) 전면 리디자인**: 좌우 2분할 대국 화면으로 재구성. `static/css/play.css` 신설(아이보리+세이지), common.css 미사용, 헤더는 `fragments/appheader`. **기존 대국 JS 유지** — 28개 id 전수 확인 후 새 레이아웃에 재배치, 7개 API(new/from/move/pass/hint/estimate/undo) 그대로. 시안의 가짜 값(Q16 61.8% 추천 목록·58% 승률·AI 난이도/덤/제한시간·alert)은 실제 데이터로 대체하거나 제거: 추천수는 `/api/play/hint` 실제 5개(승률+예상 집, 버튼 요청식 — 매 수 자동 호출은 GPU 낭비라 유지), 승률은 `/api/play/estimate`, 대국 정보는 현재 수·내 돌·흑/백 따낸 돌. 없는 항목(난이도·덤·제한시간·추천수 설명문)은 삭제. 기존 기능 전부 보존: 색 선택, 힌트 마커, 형세 판단/계가, 착수 평가, 이어두기(replaySetup), 무르기/패스/기권, 종료 모달. 검증: 실제 API 스모크(new→move→hint→estimate→undo) + 스텁으로 UI 전체 흐름 렌더, 1600/1000px, 9개 페이지 200.
- **실력 리포트(/game/result) 전면 리디자인**: 핵심 화면을 새 시안 기준으로 재구성. `static/css/result.css` 신설, common.css 미사용, 헤더는 `fragments/appheader`. **기능 JS 1,100줄은 손대지 않고 마크업/CSS만 교체** — JS 가 잡는 51개 id 전수 확인 후 새 레이아웃에 재배치. 시안의 하드코딩 값(128수 패착·3급·82% 구간·샘플 승률 배열·alert SGF)은 전부 실제 분석 JSON 값으로 대체: 총평/최종 형세/승부처, rankBand 기력, 구간별(초1~50·중51~150·종151~), AI 일치율·품질 분포(Chart.js), 패착·실수·호수 목록, flowCanvas 실측 승률. 주석 SGF 는 실제 `/game/result/{id}/sgf` 링크로 연결. 신규: 학습 요약 2칸(구간 실측값에서 문장 생성). 삭제: 옛 사이드 높이 맞춤 `syncSideHeight()`. 검증: 1600/900px 렌더, 9개 페이지+result+SGF+?move 딥링크 200, 템플릿 예외 0.
- **분석 대기 화면(/game/waiting) 전면 리디자인**: 가운데 카드 1개짜리 로딩 화면을 폐기하고 새 시안(2열 대시보드)으로 재작성. `static/css/waiting.css` 신설, 헤더는 `fragments/appheader`. 기존 폴링/완료 이동 로직은 계약 그대로 유지하고 표시만 확장: 진행률·현재 분석 수(진행률×총 수)·visits·경과 시간·남은 시간 추정·분석 과정 4단계. 시안의 5단계(가짜 "기본 형세 분석" 포함)는 실제 파이프라인 4단계로 교정. 사활 위젯은 기존 JS 240줄을 그대로 이식(.ts-* 클래스명 유지, 스타일만 새 디자인) — 난이도·이전/다음·힌트·재도전·정답 보기·정답 오버레이 전부 동작. 컨트롤러 waiting 에 totalMoves/analysisVisits/deepVisits 추가. 검증: RUNNING(45%)·DONE·UNKNOWN 3가지 상태를 status 응답 스텁으로 렌더 확인, 9개 페이지 200, 템플릿 예외 0. ※시안의 "자동 이동" 문구는 기존 동작(자동 이동 안 함)에 맞춰 수정.
## 주의/미해결
- 기존 저장 JSON 일부 구 등급(S/A/B/C/D) → 재분석해야 새 등급 반영.
